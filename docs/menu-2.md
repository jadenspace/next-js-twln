📌 PRD – 조건 필터 기반 로또 번호 조합 생성 페이지
1. 페이지 개요
1.1 목적

사용자가 통계 조건을 직접 조합하여

자신이 신뢰하는 기준에 맞는 로또 번호 조합을 생성

“자동 추천”이 아닌 제어 가능한 생성기

핵심 철학
❌ AI가 맞춘다
✅ 사용자가 조건을 정하고, 시스템은 그 조건을 만족하는 조합을 생성한다

2. 사용자 시나리오

사용자는 “번호 생성” 페이지에 진입

좌측(또는 상단)에서 조건 필터를 선택

조건을 만족하는 후보 조합 생성

결과를 검토 → 일부 조건 수정 → 재생성

마음에 드는 조합 저장 or 복사

3. 전체 화면 구조 (UX)
[ 조건 설정 영역 ]        [ 결과 영역 ]
─────────────────        ─────────────────
□ 출현 빈도 조건          • 생성된 조합 리스트
□ 홀/짝 비율              • 각 조합의 조건 충족 요약
□ 구간 분포               • 위험/균형/보수 지표
□ 미출현 번호 포함 여부
□ 회귀 조건
□ 궁합수 조건
□ 수학적 성질 조건
□ 알고리즘 선택

[ 생성 버튼 ]

4. 조건 필터 상세 정의
4.1 출현 빈도 조건
목적

“자주 나온 번호 / 덜 나온 번호” 반영

옵션

최근 N회 기준 (10 / 30 / 50 / 전체)

상위 X% 빈도 번호 포함 개수

하위 X% 빈도 번호 포함 개수

예시

상위 20% 번호 중 최소 1개

하위 30% 번호 중 최대 2개

계산 로직
frequencyRank[number] = percentile

4.2 홀 / 짝 비율 조건
옵션

3:3

4:2 / 2:4

자유 입력 (홀 최소 / 최대)

검증 로직
oddCount >= min && oddCount <= max

4.3 구간 분포 조건
목적

번호가 특정 구간에 몰리지 않도록 제어

옵션

1~10 / 11~20 / … / 41~45

구간별 최소/최대 개수

예시

1~10 구간 최대 2개

31~40 구간 최소 1개

4.4 기간별 미출현 번호 조건
옵션

최근 N회 미출현 번호 포함 여부

포함 개수 (0~N)

UX

“최근 20회 미출현 번호 중 1~2개 포함”

4.5 회귀 조건 (n회귀 통계 연동)
목적

너무 자주 나온 번호 / 너무 오래 안 나온 번호 제어

옵션

평균 회귀수 이상 번호 제외

최근 3회 이내 등장 번호 최대 N개

4.6 궁합수 조건
목적

함께 자주 등장한 번호 쌍 활용

옵션

궁합수 쌍 최소 1쌍 포함

궁합 점수 기준 이상만 허용

계산

pairFrequency[a][b] 기반

4.7 수학적 성질 조건
분류

소수

합성수

3의 배수

옵션

소수 최소 / 최대 개수

3의 배수 포함 여부

4.8 끝수 / 간격 조건 (선택)
끝수

동일 끝수 최대 N개

간격

최대 번호 – 최소 번호 범위 제한

평균 간격 범위

5. 알고리즘 선택
5.1 기본 랜덤

조건만 만족하면 균등 랜덤

5.2 가중 랜덤

빈도/회귀/궁합 점수 가중치 적용

5.3 몬테카를로 필터링

대량 랜덤 생성

조건 만족 조합만 필터

상위 점수 조합 추출

6. 번호 생성 로직 (핵심)
전체 흐름
while (results.length < targetCount) {
  candidate = generateNumbers()
  if (checkAllConditions(candidate)) {
    results.push(candidate)
  }
}

조건 충족 실패 시

조건이 너무 빡센 경우:

“조건을 완화하세요” 가이드 표시

실패 원인 표시 (ex: 구간 조건 충돌)

7. 결과 영역 설계
7.1 조합 카드 구성

번호 6개

조건 충족 요약

홀/짝

구간 분포

빈도 레벨

“보수 / 균형 / 공격” 배지

7.2 정렬 옵션

균형 점수 높은 순

랜덤

최근 미출현 가중치 높은 순

7.3 저장 / 공유

로컬 저장

번호 복사

QR or 이미지 저장 (선택)

8. UX 안정성 & 제한

❗ “예측/당첨 보장 아님” 고지

조건이 많을수록 생성 시간이 늘어남 → 로딩 표시

모바일에서는 프리셋 조건 제공

9. 기술 구현 가이드 (주니어 기준)

조건 = 순수 함수

조건 검사 함수는 단일 책임

통계 데이터는 읽기 전용

UI 상태와 생성 로직 분리